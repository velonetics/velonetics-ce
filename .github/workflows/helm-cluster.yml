name: Helm Cluster Test

on:
  push:
    branches: [main]
    paths:
      - 'deploy/helm/**'
      - 'Dockerfile'
      - 'cmd/velonetics-ce/**'
      - 'go.mod'
      - 'go.sum'
      - 'Makefile'
      - '.github/workflows/helm-cluster.yml'
  pull_request:
    branches: [main]
    paths:
      - 'deploy/helm/**'
      - 'Dockerfile'
      - 'cmd/velonetics-ce/**'
      - 'go.mod'
      - 'go.sum'
      - 'Makefile'
      - '.github/workflows/helm-cluster.yml'

jobs:
  helm-cluster:
    name: Kind install and health check
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Fetch JSON schema
        run: |
          curl -fsSL -o cmd/velonetics-ce/schema/schema.json \
            https://raw.githubusercontent.com/velonetics/velonetics-schema/v2.0.3/v2.13/velonetics.json

      - name: Vendor modules for offline Docker build
        run: GOWORK=off go mod vendor

      - name: Verify chart version alignment
        run: deploy/helm/scripts/verify-chart-version.sh

      - name: Install Helm
        uses: azure/setup-helm@v4
        with:
          version: v3.16.4

      - name: Create Kind cluster
        uses: helm/kind-action@v1
        with:
          cluster_name: velonetics-helm-test

      - name: Run cluster install test
        env:
          RELEASE_NAME: velonetics-ci
          CLUSTER_NAME: velonetics-helm-test
          SKIP_CLUSTER_CREATE: "1"
        run: deploy/helm/scripts/cluster-test.sh
