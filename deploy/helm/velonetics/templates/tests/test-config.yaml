{{- if and .Values.tests.enabled .Values.tests.configCheck (eq (include "pucora.useConfigVolume" .) "true") }}
apiVersion: v1
kind: Pod
metadata:
  name: {{ include "pucora.fullname" . }}-config-test
  labels:
    {{- include "pucora.labels" . | nindent 4 }}
  annotations:
    helm.sh/hook: test
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
spec:
  restartPolicy: Never
  containers:
    - name: config-check
      image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
      command:
        - /usr/bin/pucora
        - check
        - -c
        - /etc/pucora/pucora.json
      volumeMounts:
        - name: config
          mountPath: /etc/pucora/pucora.json
          subPath: pucora.json
          readOnly: true
  volumes:
    {{- if eq (include "pucora.useConfigMap" .) "true" }}
    - name: config
      configMap:
        name: {{ include "pucora.configMapName" . }}
    {{- else if eq (include "pucora.useConfigSecret" .) "true" }}
    - name: config
      secret:
        secretName: {{ include "pucora.configSecretName" . }}
    {{- end }}
{{- end }}
