#!/bin/sh

set -e

USER=pucora

if ! getent passwd $USER >/dev/null ; then
  adduser --system --disabled-login --gecos "Pucora" --shell /bin/false --no-create-home $USER > /dev/null
fi

if [ -x "/etc/init.d/pucora" ] || [ -e "/etc/init/pucora.conf" ]; then
	service pucora stop || exit $?
fi
